# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(octomapTaskLib)
ADD_LIBRARY(${OCTOMAP_TASKLIB_NAME} SHARED 
    ${OCTOMAP_TASKLIB_SOURCES})
add_dependencies(${OCTOMAP_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${OCTOMAP_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${OCTOMAP_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${OCTOMAP_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${OCTOMAP_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${OCTOMAP_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${OCTOMAP_TASKLIB_HEADERS}
    DESTINATION include/orocos/octomap)

